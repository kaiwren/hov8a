# frozen_string_literal: true

RSpec.describe Hov8a do
  it "has a version number" do
    expect(Hov8a::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
